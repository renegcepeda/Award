1. **Open Terminal**
   Use Spotlight (`Cmd + Space`) and search for "Terminal".

2. **Navigate to the Project Directory**
   cd ~/Desktop/Award

git add .
git commit -m "Complete redesign, archived prior version under folder OLD"
git push

