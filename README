1. **Open Terminal**
   Use Spotlight (`Cmd + Space`) and search for "Terminal".

2. **Navigate to the Project Directory**
   cd ~/Desktop/Awards

git add .
git commit -m "Update: made changes to AwardShow_2025, backup, and added README"
git push

